# Optional: a Formspree endpoint (https://formspree.io/f/xxxxxxx) or any
# endpoint that accepts a JSON POST with { name, email, company, projectType,
# message }. When unset, the contact form falls back to opening the
# visitor's email client instead of pretending to submit online.
VITE_FORM_ENDPOINT=
